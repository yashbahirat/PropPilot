// PropPilot Prisma Schema
// Datasource only — full model definitions added by Plan 04

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
