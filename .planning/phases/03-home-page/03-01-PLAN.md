---
phase: 03-home-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/home/Hero.tsx, src/app/(marketing)/page.tsx]
autonomous: true
requirements: [HOME-01, HOME-02, HOME-08, HOME-09]

must_haves:
  truths:
    - "User can see a premium hero section with gradient mesh"
    - "User can search and be redirected to /compare"
    - "User can see footer"
  artifacts:
    - path: "src/components/home/Hero.tsx"
      provides: "Hero section"
    - path: "src/app/(marketing)/page.tsx"
      provides: "Home page"
  key_links:
    - from: "src/components/home/Hero.tsx"
      to: "/compare"
      via: "Search form submission"
---

<objective>
Build the Hero section with a glow gradient mesh and search functionality, and update the home page to render it. Include the already-built Footer to satisfy HOME-08.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-home-page/03-CONTEXT.md
@.planning/phases/03-home-page/03-UI-SPEC.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build Hero Component</name>
  <files>src/components/home/Hero.tsx</files>
  <read_first>
    - src/components/home/Hero.tsx
    - .planning/phases/03-home-page/03-UI-SPEC.md
  </read_first>
  <action>
    Create the Hero component implementing D-01 (Glow gradient mesh background) and D-03 (Search redirects to `/compare?q=`). Include CTA buttons: "Compare Firms", "Browse Discounts", "Join/Login". Use Framer Motion (`LazyMotion` + `domAnimation`) for subtle entrance animations (HOME-09). The component must be `"use client"` because of form submission and animations. Provide concrete placeholder text and use shadcn/ui components (Button, Input) internally if applicable.
  </action>
  <verify>
    <automated>grep -q "Compare Firms" src/components/home/Hero.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/home/Hero.tsx contains "use client"
    - src/components/home/Hero.tsx contains the exact string "Compare Firms"
    - src/components/home/Hero.tsx contains the exact string "Browse Discounts"
    - src/components/home/Hero.tsx contains the exact string "Join/Login"
    - src/components/home/Hero.tsx contains an HTML form or router push logic for search.
  </acceptance_criteria>
  <done>Hero component created with mesh background and working search form.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Hero into Home Page</name>
  <files>src/app/(marketing)/page.tsx</files>
  <read_first>
    - src/app/(marketing)/page.tsx
    - src/components/layout/Footer.tsx
  </read_first>
  <action>
    Update `src/app/(marketing)/page.tsx` to remove the temporary placeholder and render the `<Hero />` component. Also ensure `<Footer />` is rendered if it's not already in the layout, to satisfy HOME-08. Ensure the page layout can accommodate subsequent sections.
  </action>
  <verify>
    <automated>grep -q "Hero" src/app/(marketing)/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(marketing)/page.tsx contains `<Hero`
  </acceptance_criteria>
  <done>Home page renders the Hero component.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client to Server | Search query string is appended to URL |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Search input | accept | Input is only passed as query param to next page where it will be validated. |
</threat_model>

<verification>
- Hero renders with gradient mesh.
- Submitting search redirects to `/compare?q=term`.
</verification>

<success_criteria>
Home page displays the Hero section perfectly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-page/03-01-SUMMARY.md`
</output>
