---
wave: 4
depends_on: []
files_modified:
  - src/components/home/FeaturedDeals.tsx
  - src/components/home/TrustMetrics.tsx
  - src/app/globals.css
autonomous: true
---

# Phase 03: Home Page (Gap Closure)

This plan addresses the UI and interaction gaps discovered during Phase 03 UAT.

## Tasks

```xml
<task>
  <id>gap-featured-deals-scroll</id>
  <title>Enable Drag Scrolling for Featured Deals</title>
  <description>Add Framer Motion drag props to the Featured Deals carousel so it can be scrolled via mouse drag on desktop.</description>
  <read_first>
    - src/components/home/FeaturedDeals.tsx
  </read_first>
  <action>
    Modify `src/components/home/FeaturedDeals.tsx` to enable mouse drag:
    1. Import `useRef` from 'react'.
    2. Create a ref for the carousel container: `const carouselRef = useRef<HTMLDivElement>(null)`.
    3. Attach the ref to the inner div that wraps the items: `<div ref={carouselRef} className="flex ...">`.
    4. Update the `<m.div>` for each item to include:
       - `drag="x"`
       - `dragConstraints={carouselRef}`
       - `dragElastic={0.1}`
    5. Note: Since we have native `overflow-x-auto` and `snap-x`, adding `drag="x"` allows mouse dragging, but we might need to remove `snap-mandatory` if it conflicts with Framer Motion drag. Just add the drag props to improve the desktop interaction.
  </action>
  <acceptance_criteria>
    - `src/components/home/FeaturedDeals.tsx` contains `drag="x"` and `dragConstraints`.
  </acceptance_criteria>
</task>

<task>
  <id>gap-trust-metrics-animate</id>
  <title>Animate Trust Metrics Counters</title>
  <description>Animate the numbers in the Trust Metrics section from 0 to their target values when they scroll into view.</description>
  <read_first>
    - src/components/home/TrustMetrics.tsx
  </read_first>
  <action>
    Modify `src/components/home/TrustMetrics.tsx`:
    1. Import `useMotionValue`, `useTransform`, `animate`, `useInView` from `framer-motion`, and `useEffect`, `useRef` from `react`.
    2. Since the values contain text like "150+", "12K+", "45", parse out the numeric parts (150, 12, 45) for animation, and append the suffixes in the `useTransform`.
    3. Create a sub-component `<AnimatedCounter value={150} suffix="+" label="Verified Deals" />` or integrate the logic inside the map using a small wrapper component to handle its own `useInView` and `animate`.
    4. For example, if value is "150+", target=150, suffix="+". If "12K+", target=12, suffix="K+".
    5. In the wrapper component:
       `const count = useMotionValue(0);`
       `const rounded = useTransform(count, (latest) => Math.round(latest) + suffix);`
       `useEffect(() => { if (isInView) animate(count, target, { duration: 2 }); }, [isInView])`
       Render `<m.h3>{rounded}</m.h3>`.
  </action>
  <acceptance_criteria>
    - `src/components/home/TrustMetrics.tsx` contains `useMotionValue` and `animate`.
  </acceptance_criteria>
</task>

<task>
  <id>gap-home-faq-animations</id>
  <title>Add Accordion Animations to globals.css</title>
  <description>Add the missing shadcn/ui accordion CSS animations to the Tailwind 4 globals.css so the Home FAQ expands smoothly.</description>
  <read_first>
    - src/app/globals.css
  </read_first>
  <action>
    Modify `src/app/globals.css`:
    1. Inside the `@theme` block, add the following CSS variables for animations:
       `--animate-accordion-down: accordion-down 0.2s ease-out;`
       `--animate-accordion-up: accordion-up 0.2s ease-out;`
    2. Also inside the `@theme` block, add the keyframes:
       `@keyframes accordion-down { from { height: 0; } to { height: var(--radix-accordion-content-height); } }`
       `@keyframes accordion-up { from { height: var(--radix-accordion-content-height); } to { height: 0; } }`
  </action>
  <acceptance_criteria>
    - `src/app/globals.css` contains `--animate-accordion-down` and `@keyframes accordion-down`.
  </acceptance_criteria>
</task>
```

## Verification
- Verify `FeaturedDeals.tsx` has `drag` props.
- Verify `TrustMetrics.tsx` uses `useMotionValue`.
- Verify `globals.css` has accordion animations.
