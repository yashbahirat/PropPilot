---
wave: 6
depends_on: ["02-01"]
files_modified: ["src/app/(admin)/admin/reviews/page.tsx", "src/app/(admin)/admin/reviews/actions.ts"]
autonomous: true
---

# 02-06: Review Moderation

## Goal
Admin interface to list pending user reviews and approve or reject them.

## Context
- `Review` schema has `isApproved` flag.

## Tasks

<task>
<read_first>
- src/app/(admin)/admin/reviews/page.tsx
</read_first>
<action>
Implement the Reviews list view at `src/app/(admin)/admin/reviews/page.tsx`.
- Fetch `db.review.findMany({ include: { firm: true } })`.
- Display a data table or list showing: Firm Name, Rating, Title, Content, and Status (Pending/Approved).
- Add "Approve" and "Reject/Delete" action buttons.
</action>
<acceptance_criteria>
- `page.tsx` calls `db.review.findMany`
- Renders `Firm` relationship data
</acceptance_criteria>
</task>

<task>
<read_first>
- src/app/(admin)/admin/reviews/actions.ts
</read_first>
<action>
Create Server Actions in `src/app/(admin)/admin/reviews/actions.ts`.
- `approveReview(id: string)`: Sets `isApproved = true`.
- `deleteReview(id: string)`: Deletes the review from the database.
Wire these actions to the buttons in the list view.
</action>
<acceptance_criteria>
- `actions.ts` exports `approveReview`
- `actions.ts` exports `deleteReview`
</acceptance_criteria>
</task>

## Verification
1. Admin can navigate to `/admin/reviews` and see a list of reviews.
2. Clicking "Approve" successfully updates the review status.
