---
wave: 2
depends_on: ["02-01"]
files_modified: ["src/app/(admin)/admin/firms/page.tsx", "src/components/admin/FirmTable.tsx", "src/components/admin/columns.tsx"]
autonomous: true
---

# 02-02: Firm Management Data Table

## Goal
Create a data table view of all prop firms for the admin dashboard.

## Context
- The user chose a Data Table for the firm list view.
- Requires `@tanstack/react-table` for sorting/filtering.
- Data fetching should be handled with server components.

## Tasks

<task>
<read_first>
- package.json
</read_first>
<action>
Run `npx shadcn@latest add table dropdown-menu` to install the required shadcn components if not already installed. Also ensure `@tanstack/react-table` is installed.
</action>
<acceptance_criteria>
- `components/ui/table.tsx` exists
- `@tanstack/react-table` is in `package.json`
</acceptance_criteria>
</task>

<task>
<read_first>
- src/app/(admin)/admin/firms/page.tsx
- prisma/schema.prisma
</read_first>
<action>
Create `src/components/admin/columns.tsx`.
Define the columns for the Firm table: `name`, `isActive`, `isFeatured`, `isVerified`, `createdAt`, and an `actions` column with a dropdown menu linking to the edit page (`/admin/firms/[id]`).
</action>
<acceptance_criteria>
- `columns.tsx` exports a `columns` array of type `ColumnDef<Firm>`
</acceptance_criteria>
</task>

<task>
<read_first>
- src/components/admin/columns.tsx
</read_first>
<action>
Create `src/components/admin/FirmTable.tsx` implementing a reusable data table using `@tanstack/react-table` and `shadcn` Table components. It should accept `data` and `columns` props.
</action>
<acceptance_criteria>
- `FirmTable.tsx` imports and uses `useReactTable`
</acceptance_criteria>
</task>

<task>
<read_first>
- src/app/(admin)/admin/firms/page.tsx
- src/components/admin/FirmTable.tsx
- src/lib/db.ts
</read_first>
<action>
Implement the `src/app/(admin)/admin/firms/page.tsx` page.
- This is a Server Component.
- Fetch all firms from Prisma `db.firm.findMany({ orderBy: { createdAt: 'desc' } })`.
- Render the `FirmTable` passing the fetched data and the columns.
- Add an "Add Firm" button linking to `/admin/firms/new`.
</action>
<acceptance_criteria>
- `page.tsx` calls `db.firm.findMany`
- `page.tsx` renders `<FirmTable />`
</acceptance_criteria>
</task>

## Verification
1. Navigate to `/admin/firms`.
2. The table should correctly render any firms from the database (initially empty).
3. The "Add Firm" button should be visible.
