---
wave: 5
depends_on: ["02-01"]
files_modified: ["src/app/(admin)/admin/guides/page.tsx", "src/app/(admin)/admin/guides/[id]/page.tsx", "src/components/admin/GuideForm.tsx"]
autonomous: true
---

# 02-05: Guide Post Management

## Goal
Admin interface to list, create, and edit educational guide posts.

## Context
- `GuidePost` schema has `title`, `slug`, `content`, `category`, `isPublished`.

## Tasks

<task>
<read_first>
- src/app/(admin)/admin/guides/page.tsx
</read_first>
<action>
Implement the Guide list view at `src/app/(admin)/admin/guides/page.tsx`.
- Fetch `db.guidePost.findMany()`.
- Display a basic data table or list with columns: Title, Status (Published/Draft), Category, Actions.
- Add an "Add Guide" button.
</action>
<acceptance_criteria>
- `page.tsx` calls `db.guidePost.findMany`
- Table/list renders guides
</acceptance_criteria>
</task>

<task>
<read_first>
- prisma/schema.prisma
</read_first>
<action>
Create `src/components/admin/GuideForm.tsx`.
- Form fields: `title`, `slug`, `excerpt`, `content` (textarea for now, MDX/RichText later if needed), `category`, `isPublished`, `authorName`.
</action>
<acceptance_criteria>
- `GuideForm.tsx` exists with standard form fields
</acceptance_criteria>
</task>

<task>
<read_first>
- src/app/(admin)/admin/guides/[id]/page.tsx
- src/components/admin/GuideForm.tsx
</read_first>
<action>
Implement the Guide edit/create page at `src/app/(admin)/admin/guides/[id]/page.tsx`.
- Handle `new` vs existing ID similar to the firm page.
- Create Server Actions in `src/app/(admin)/admin/guides/actions.ts` for saving and deleting guides.
</action>
<acceptance_criteria>
- `actions.ts` has `saveGuide`
- `page.tsx` fetches `db.guidePost.findUnique`
</acceptance_criteria>
</task>

## Verification
1. Admin can navigate to `/admin/guides` and see a list of guides.
2. Creating a new guide saves it to the database with the correct published status.
