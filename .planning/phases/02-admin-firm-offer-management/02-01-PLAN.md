---
wave: 1
depends_on: []
files_modified: ["src/app/(admin)/layout.tsx", "src/components/layout/AdminSidebar.tsx", "src/app/(admin)/page.tsx"]
autonomous: true
---

# 02-01: Admin Dashboard Layout

## Goal
Implement the admin layout wrapper and sidebar navigation.

## Context
- Admin routes must be visually separate from the marketing site (already configured with a minimal `AdminLayout` wrapper).
- Uses `shadcn/ui` components for navigation items.

## Tasks

<task>
<read_first>
- src/app/(admin)/layout.tsx
- .planning/phases/02-admin-firm-offer-management/02-UI-SPEC.md
</read_first>
<action>
Create `src/components/layout/AdminSidebar.tsx`.
Implementation details:
- Use a `w-64` fixed sidebar layout for desktop, hidden on mobile (mobile will use a sheet or be deferred).
- Include standard navigation links: Dashboard (`/admin`), Firms (`/admin/firms`), Reviews (`/admin/reviews`), Guides (`/admin/guides`).
- Use `lucide-react` icons for each link (`LayoutDashboard`, `Building2`, `MessageSquare`, `BookOpen`).
- Style with dark mode tokens (`bg-[#08080F]`, active items `text-[#00D4AA]`).
</action>
<acceptance_criteria>
- `AdminSidebar.tsx` contains links to `/admin`, `/admin/firms`, `/admin/reviews`, `/admin/guides`
</acceptance_criteria>
</task>

<task>
<read_first>
- src/app/(admin)/layout.tsx
- src/components/layout/AdminSidebar.tsx
</read_first>
<action>
Update `src/app/(admin)/layout.tsx` to include the `AdminSidebar`.
Implementation details:
- Flex layout: Sidebar on the left, main content area (`flex-1`) on the right.
- Apply `bg-[#0E0E1A]` to the main content area.
</action>
<acceptance_criteria>
- `layout.tsx` imports and renders `<AdminSidebar />`
</acceptance_criteria>
</task>

<task>
<read_first>
- src/app/(admin)/page.tsx
</read_first>
<action>
Create a placeholder dashboard overview at `src/app/(admin)/page.tsx`.
Implementation details:
- Display a heading "Admin Dashboard".
- Add 4 basic metric cards (using shadcn `Card`) for "Total Firms", "Pending Reviews", "Active Offers", "Total Guides" (mock data for now, value "0").
</action>
<acceptance_criteria>
- `page.tsx` renders 4 `Card` components
</acceptance_criteria>
</task>

## Verification
1. Accessing `/admin` displays the sidebar with working navigation links.
2. The layout strictly adheres to the dark mode PropPilot token colors.
