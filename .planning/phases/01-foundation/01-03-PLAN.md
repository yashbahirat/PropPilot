---
id: "01-03"
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/app/globals.css
  - tailwind.config.ts
autonomous: true
requirements_addressed: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, HOME-09]
---

# Plan 03: Design System — Tailwind Config & Global CSS

## Objective
Implement the full PropPilot design system: override all shadcn/ui CSS tokens, define the brand color palette (teal/cyan/mint/purple on deep dark base), Inter typography, glassmorphism utilities, and all Tailwind extensions defined in the UI-SPEC.

## Tasks

<task id="1">
<action>
Replace `tailwind.config.ts` with the full PropPilot design system config:

```typescript
import type { Config } from 'tailwindcss'

const config: Config = {
  darkMode: ['class'],
  content: [
    './src/pages/**/*.{ts,tsx}',
    './src/components/**/*.{ts,tsx}',
    './src/app/**/*.{ts,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        // shadcn/ui CSS variable tokens
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))',
        },
        popover: {
          DEFAULT: 'hsl(var(--popover))',
          foreground: 'hsl(var(--popover-foreground))',
        },
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))',
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
        destructive: {
          DEFAULT: 'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))',
        },
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        // PropPilot brand palette
        teal: {
          primary: '#00D4AA',
          dim: '#00A882',
          deep: '#007A5E',
          subtle: 'rgba(0,212,170,0.08)',
          glow: 'rgba(0,212,170,0.20)',
        },
        cyan: { accent: '#22D3EE' },
        mint: { accent: '#6EE7B7' },
        purple: {
          accent: '#8B5CF6',
          dim: '#6D28D9',
          subtle: 'rgba(139,92,246,0.12)',
        },
        surface: {
          DEFAULT: '#0E0E1A',
          2: '#131320',
          3: '#1A1A2E',
        },
        'prop-bg': '#08080F',
        'prop-border': '#1E1E30',
        'prop-border-subtle': '#161625',
      },
      fontFamily: {
        sans: ['var(--font-inter)', 'system-ui', '-apple-system', 'sans-serif'],
        mono: ['JetBrains Mono', 'Fira Code', 'monospace'],
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
      letterSpacing: {
        tightest: '-0.04em',
        tighter: '-0.03em',
      },
      backdropBlur: { xs: '2px' },
      boxShadow: {
        'teal-glow': '0 0 20px rgba(0,212,170,0.25), 0 0 60px rgba(0,212,170,0.08)',
        'teal-glow-sm': '0 0 10px rgba(0,212,170,0.20)',
        glass: 'inset 0 1px 0 0 rgba(255,255,255,0.05)',
        'glass-teal': 'inset 0 1px 0 0 rgba(0,212,170,0.1)',
        card: '0 4px 24px rgba(0,0,0,0.4)',
        'card-hover': '0 8px 40px rgba(0,0,0,0.5), 0 0 0 1px rgba(0,212,170,0.1)',
      },
      animation: {
        'slide-in-right': 'slideInRight 0.3s cubic-bezier(0.16, 1, 0.3, 1)',
        'slide-out-right': 'slideOutRight 0.25s cubic-bezier(0.16, 1, 0.3, 1)',
        'fade-in': 'fadeIn 0.2s ease-out',
        'fade-out': 'fadeOut 0.15s ease-in',
        'scale-in': 'scaleIn 0.2s cubic-bezier(0.16, 1, 0.3, 1)',
        'pulse-teal': 'pulseTeal 2s ease-in-out infinite',
      },
      keyframes: {
        slideInRight: {
          from: { transform: 'translateX(100%)', opacity: '0' },
          to: { transform: 'translateX(0)', opacity: '1' },
        },
        slideOutRight: {
          from: { transform: 'translateX(0)', opacity: '1' },
          to: { transform: 'translateX(100%)', opacity: '0' },
        },
        fadeIn: {
          from: { opacity: '0', transform: 'translateY(4px)' },
          to: { opacity: '1', transform: 'translateY(0)' },
        },
        fadeOut: { from: { opacity: '1' }, to: { opacity: '0' } },
        scaleIn: {
          from: { transform: 'scale(0.96)', opacity: '0' },
          to: { transform: 'scale(1)', opacity: '1' },
        },
        pulseTeal: {
          '0%, 100%': { opacity: '1' },
          '50%': { opacity: '0.6' },
        },
      },
    },
  },
  plugins: [require('@tailwindcss/typography')],
}

export default config
```
</action>
<read_first>
- `tailwind.config.ts` (existing content from shadcn init — will be fully replaced)
- `.planning/phases/01-foundation/01-UI-SPEC.md` (canonical color values and animation specs)
</read_first>
<acceptance_criteria>
- `tailwind.config.ts` contains `darkMode: ['class']`
- `tailwind.config.ts` contains `teal: { primary: '#00D4AA'`
- `tailwind.config.ts` contains `purple: { accent: '#8B5CF6'`
- `tailwind.config.ts` contains `'prop-bg': '#08080F'`
- `tailwind.config.ts` contains `pulseTeal` in keyframes
- `tailwind.config.ts` contains `plugins: [require('@tailwindcss/typography')]`
</acceptance_criteria>
</task>

<task id="2">
<action>
Replace `src/app/globals.css` with the full PropPilot design system CSS. This overrides all shadcn/ui tokens with PropPilot's brand values and adds all utility classes:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 240 43% 4%;
    --foreground: 210 40% 95%;
    --card: 240 33% 8%;
    --card-foreground: 210 40% 95%;
    --popover: 240 30% 10%;
    --popover-foreground: 210 40% 95%;
    --primary: 167 100% 42%;
    --primary-foreground: 168 100% 5%;
    --secondary: 240 24% 15%;
    --secondary-foreground: 210 40% 75%;
    --muted: 240 27% 14%;
    --muted-foreground: 215 20% 55%;
    --accent: 167 100% 42%;
    --accent-foreground: 168 100% 5%;
    --destructive: 0 91% 71%;
    --destructive-foreground: 210 40% 98%;
    --border: 240 24% 15%;
    --input: 240 27% 14%;
    --ring: 167 100% 42%;
    --radius: 0.625rem;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  html {
    color-scheme: dark;
  }
  body {
    @apply bg-background text-foreground;
    font-feature-settings: 'cv11', 'ss01';
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }
}

@layer utilities {
  /* Glassmorphism surfaces */
  .glass-card {
    background: rgba(14, 14, 26, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.06);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
  }
  .glass-nav {
    background: rgba(8, 8, 15, 0.8);
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }
  .glass-modal {
    background: rgba(13, 13, 25, 0.97);
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
  }
  .glass-teal {
    background: rgba(0, 212, 170, 0.06);
    border: 1px solid rgba(0, 212, 170, 0.15);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }

  /* Glow effects */
  .teal-glow {
    box-shadow: 0 0 20px rgba(0, 212, 170, 0.25), 0 0 60px rgba(0, 212, 170, 0.08);
  }
  .teal-glow-sm {
    box-shadow: 0 0 10px rgba(0, 212, 170, 0.2);
  }

  /* Gradient text */
  .gradient-text-teal {
    background: linear-gradient(135deg, #00D4AA 0%, #22D3EE 50%, #6EE7B7 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .gradient-text-brand {
    background: linear-gradient(135deg, #00D4AA 0%, #8B5CF6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }
}
```
</action>
<read_first>
- `src/app/globals.css` (existing shadcn-generated content — will be fully replaced)
- `.planning/phases/01-foundation/01-UI-SPEC.md` §shadcn/ui Theme Tokens (exact HSL values)
</read_first>
<acceptance_criteria>
- `src/app/globals.css` contains `--background: 240 43% 4%`
- `src/app/globals.css` contains `--primary: 167 100% 42%`
- `src/app/globals.css` contains `--ring: 167 100% 42%`
- `src/app/globals.css` contains `.glass-card` class with `backdrop-filter: blur(16px)`
- `src/app/globals.css` contains `.glass-nav` class
- `src/app/globals.css` contains `.gradient-text-teal` class
- `src/app/globals.css` contains `color-scheme: dark`
- `src/app/globals.css` contains `prefers-reduced-motion` media query
</acceptance_criteria>
</task>

## Verification

```bash
# Tailwind config is valid TypeScript
npx tsc tailwind.config.ts --noEmit --skipLibCheck 2>&1 | grep -v "^$" || true

# CSS variables defined
grep -c "^  --" src/app/globals.css

# Glass utilities present
grep "glass-card\|glass-nav\|glass-modal\|glass-teal" src/app/globals.css | wc -l

# Next.js build includes CSS
npx next build 2>&1 | grep -E "compiled|error" | tail -5
```

## must_haves
- `tailwind.config.ts` has `darkMode: ['class']` and full PropPilot palette
- `globals.css` has all shadcn/ui tokens overridden with PropPilot HSL values
- All four glassmorphism utility classes defined: `.glass-card`, `.glass-nav`, `.glass-modal`, `.glass-teal`
- Gradient text utilities and reduced-motion support included

<threat_model>
## Security Threat Model

| Threat | Severity | Mitigation |
|--------|----------|-----------|
| CSS injection via user-supplied class names | Low | Tailwind's JIT mode only generates classes present in source — no dynamic class injection |
| CDN font loading intercepted | Low | Use `next/font/google` (subsets fonts locally) rather than Google Fonts CDN link |
</threat_model>
